/**
 * Consultation Details Route (Plan 2 only)
 */
import { ConsultationDetailsScreen } from '@features/verification/ConsultationDetailsScreen';

export default function ConsultationDetailsRoute() {
  return <ConsultationDetailsScreen />;
}
