/**
 * Verification Landing Route (Choose Plan)
 */
import { VerificationLandingScreen } from '@features/verification/VerificationLandingScreen';

export default function VerificationLandingRoute() {
  return <VerificationLandingScreen />;
}
